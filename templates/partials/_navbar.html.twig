<div class="h-12 bg-green-400 flex w-full items-center px-4">
    <a href="{{ path('home') }}" class="text-white font-bold">Hadrien Delphin : Développeur freelance</a>
    <select class="ml-5 p-2 border-gray-200 rounded-lg bg-gray-100" name="categories" id="categories">
        {% for category in categories %}

            <option value="{{ category.id }}">{{ category.title }}</option>

        {% endfor %}
    </select>
</div>
