{% extends 'base.html.twig' %}

{% block body %}

    <h3>
        {{ skill.title }}
    </h3>

    <h4>
        Technos liées : {% for techno in skill.technos %} {{ techno.title}} {% endfor %}
    </h4>

    <p>
        {{ skill.content }}
    </p>

{% endblock %}
